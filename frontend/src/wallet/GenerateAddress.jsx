import React from 'react';
import { Button } from 'antd';

function GenerateAddress() {
	const generate = () => {};
	return <Button onClick={() => generate()}>Generate Address</Button>;
}

export default GenerateAddress;
